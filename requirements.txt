============================================================
   GUARDRAIL SENTINEL — PLAYWRIGHT POC
   Node.js / npm Project (NOT Python)
============================================================

⚠️  This is a Node.js project. Do NOT use pip.

Install using npm:
  npm install
  npx playwright install chromium

============================================================
SYSTEM REQUIREMENTS
============================================================
Node.js >= 18.0.0
  Download from: https://nodejs.org/en/download
  Verify: node --version

npm >= 9.0.0
  Bundled with Node.js
  Verify: npm --version

============================================================
npm PACKAGES (see package.json)
============================================================
@playwright/test  ^1.44.0
@types/node       ^20.0.0
typescript        ^5.4.0

============================================================
PLAYWRIGHT BROWSER
============================================================
chromium
  Install: npx playwright install chromium

============================================================
QUICK START
============================================================
1. cd C:\Users\AH012\OneDrive\Desktop\Automation_testing
2. npm install
3. npx playwright install chromium
4. npm test
